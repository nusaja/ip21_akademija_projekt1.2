<!doctype html>
<html lang="en">

<head>

    <link rel="stylesheet" href="/style.css">  
    <meta charset="utf-8">

    <title>My Webpage</title>
</head>

<body>

    <h1>List of cat and dog breeds</h1>

    <ul id="breeds">
    {% for entry in data %}

        <li>{% if entry.type == "dog"%}<img src = "/dog-alt.svg"/>  {% else %}<img src = "/cat-alt-2.svg">{% endif %}{{ entry.name }}</li>

    {% endfor %}
    </ul>

</body>

</html>